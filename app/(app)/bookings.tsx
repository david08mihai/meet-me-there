import { Placeholder } from '../../src/ui/Placeholder';

export default function Bookings() {
  return (
    <Placeholder
      title="Bookings"
      owner="P3 · Social + Profile"
      description="Participation history (past and upcoming events). Spec: [Social] See Bookings."
    />
  );
}
