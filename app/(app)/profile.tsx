import { Placeholder } from '../../src/ui/Placeholder';

export default function Profile() {
  return (
    <Placeholder
      title="Profile"
      owner="P3 · Social + Profile"
      description="User profile (photo, name, phone, date of birth, gender, Trust Score). Spec: [Profile] See Profile."
    />
  );
}
