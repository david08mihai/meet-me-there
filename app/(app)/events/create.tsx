import { Placeholder } from '../../../src/ui/Placeholder';

export default function CreateEvent() {
  return (
    <Placeholder
      title="Create Event"
      owner="P2 · Events + Map"
      description="Event creation form (title, location, time, capacity, ticket, etc.). Spec: [Events] Create Event."
    />
  );
}
