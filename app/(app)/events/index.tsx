import { Placeholder } from '../../../src/ui/Placeholder';

export default function EventsList() {
  return (
    <Placeholder
      title="Events"
      owner="P2 · Events + Map"
      description="List of events + entry point to /events/create. Spec: [Events] Create Event, [Events] See Event Details."
    />
  );
}
