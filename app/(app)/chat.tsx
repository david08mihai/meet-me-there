import { Placeholder } from '../../src/ui/Placeholder';

export default function Chat() {
  return (
    <Placeholder
      title="Chat"
      owner="P3 · Social + Profile"
      description="Group chat for event participants (likely Firestore-backed). Spec: [Social] Chat."
    />
  );
}
