import { Placeholder } from '../../../src/ui/Placeholder';

export default function RegisterBusiness() {
  return (
    <Placeholder
      title="Register — Business Account"
      owner="P1 · Auth"
      description="Fields: Logo (optional), Business Name, Category, Short Description, Contact Person, Phone, Business Email, Password, Website (optional), Social Media (optional), Accept Terms. Spec: [Auth] Register (Business)."
    />
  );
}
