import { Placeholder } from '../../../src/ui/Placeholder';

export default function RegisterPersonal() {
  return (
    <Placeholder
      title="Register — Personal Account"
      owner="P1 · Auth"
      description="Fields: Photo (optional), Email, Password, Confirm Password, Name, Phone, Date of Birth, Gender, Accept Terms. Use validators from src/lib/validation.ts. Spec: [Auth] Register (Personal)."
    />
  );
}
