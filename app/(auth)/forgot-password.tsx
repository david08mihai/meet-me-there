import { Placeholder } from '../../src/ui/Placeholder';

export default function ForgotPassword() {
  return (
    <Placeholder
      title="Forgot Password"
      owner="P1 · Auth"
      description="Implement password reset via Firebase sendPasswordResetEmail. Spec: [Auth] Forgot Password."
    />
  );
}
